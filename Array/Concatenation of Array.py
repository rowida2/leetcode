class Solution:
    def getConcatenation(self, nums: List[int]) -> List[int]:
      n = length(nums)
      ans = empty list

      for i in range(n):
        ans.append(nums[i])

      for i in range(n):
        ans.append(nums[i])

     return ans
